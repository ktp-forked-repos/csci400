class BSearch
  def chop(target, sorted_array)
    # Returns the index of the target if it exists in the list, 
    # otherwise returns -1. (Even though you can just use list.target
    # for this in real life.)
    
  end
end
